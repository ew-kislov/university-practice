{
  "name": "task1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:sailor:dev": "ts-node src/sailor.ts",
    "start:sailor:watch": "nodemon src/sailor.ts --ignore '*.json' --exec ts-node src/sailor.ts",
    "start:broker-proxy:dev": "ts-node src/broker-proxy.ts",
    "start:broker-proxy:watch": "nodemon src/broker-proxy.ts --ignore '*.json' --exec ts-node src/broker-proxy.ts",
    "start:starter:dev": "ts-node src/starter.ts",
    "start:starter:watch": "nodemon src/starter.ts --ignore '*.json' --exec ts-node src/starter.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.8.0",
    "typescript": "^4.3.4"
  },
  "devDependencies": {
    "@types/amqplib": "^0.8.2",
    "nodemon": "^2.0.7",
    "tsc": "^2.0.3"
  }
}
